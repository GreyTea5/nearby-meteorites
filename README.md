# find nearby meteorites
Example project to find nearest meteor strikes.

##Running
This project requires python and the requests module

First, install pipenv. Then:

```
pipenv install
pipenv run "python find_meteors.py"
```
